# Navigation
alias ..="cd .."

# mv, rm, cp
alias mv='mv -v'
alias rm='rm -i -v'
alias cp='cp -v'

# Typos
alias brwe=brew 

alias hosts='sudo $EDITOR /etc/hosts'

# Set python3
alias python=python3

# Networking
alias wget="curl -O"


